const BODY = document.body;
const Form = document.querySelector('.mainform');
const SubmitBtn = Form.querySelector('.submitbtn');

SubmitBtn.addEventListener("click", () => {
    const InputData = document.querySelectorAll("[required]");
})